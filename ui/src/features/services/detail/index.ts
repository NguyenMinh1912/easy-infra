// Public surface of the service detail screen. The services barrel re-exports
// this so the app shell can route `#/profiles/{p}/services/{name}` to it.
export { ServiceDetailPage } from "./ServiceDetailPage";
export { ServiceSettingsDialog } from "./ServiceSettingsDialog";
